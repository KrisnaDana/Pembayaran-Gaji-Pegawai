<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="id.krisnadana.title">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Title">
        <activity
            android:name=".DetailPegawaiBayarActivity"
            android:exported="false" />
        <activity
            android:name=".DetailPegawaiActivity"
            android:exported="false" />
        <activity
            android:name=".SudahDigajiActivity"
            android:exported="false" />
        <activity
            android:name=".BelumDigajiActivity"
            android:exported="false" />
        <activity
            android:name=".PilihBulanActivity"
            android:exported="false" />

        <activity
            android:name=".LoginManagerActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>